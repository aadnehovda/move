## Summary
- Add verbose logging for empty source directories pruned by `--prune-empty-dirs`.
- Add integration coverage to verify the prune-directory log output.

## Changes
- Emit `{"op": "prune-dir"}` debug lines for each deleted source directory when `--verbose` is enabled.
- Add an integration test covering the verbose prune-directory output.
- Keep the unrelated README positioning edits out of this release.

## Verification
- `dotnet build move.slnx`
- `dotnet test move.slnx`
