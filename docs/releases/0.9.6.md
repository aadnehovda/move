## Summary
- Remove the unnecessary async-enumerable adapter from file enumeration while keeping lazy traversal and parallel processing.
- Require checked-in, agent-authored release notes for each tagged release.
- Teach the release workflow to fail when a versioned release note file is missing or trivial.

## Changes
- Simplify file enumeration in `src/move/Program.cs` by using a normal lazy `IEnumerable<FileInfo>`.
- Add `AGENTS.md` guidance requiring `docs/releases/<tag>.md` before tagging.
- Update the release workflow to publish `docs/releases/<version>.md` and verify that the body is meaningful.

## Verification
- `dotnet build move.slnx`
- `dotnet test move.slnx`
